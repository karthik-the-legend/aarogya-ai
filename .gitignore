# ── Secrets (NEVER commit these) ────────────────
.env
*.key
secrets/

# ── Virtual Environment ──────────────────────────
env/
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# ── FAISS Index (rebuild from PDFs) ─────────────
vectorstore/

# ── VS Code ──────────────────────────────────────
.vscode/
!.vscode/settings.json

# ── Large model files ────────────────────────────
*.bin
*.pt
*.onnx
models/

# ── Logs ─────────────────────────────────────────
logs/*.json
logs/*.csv
!logs/.gitkeep

# ── OS garbage ───────────────────────────────────
.DS_Store
Thumbs.db